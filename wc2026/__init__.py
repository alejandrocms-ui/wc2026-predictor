"""wc2026-predictor — calibrated probabilistic predictions for the 2026 FIFA World Cup.

This package is structured as an importable library so a future FastAPI + React front
end can reuse the prediction/simulation logic without a rewrite. The Streamlit app in
``wc2026.app`` is only a thin presentation layer over this library (see README §seam).
"""

__version__ = "0.1.0"
